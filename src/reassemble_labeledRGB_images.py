

def reassemble_image(labeled_rgb_path, reconstructed_output_path):   
    """
    This algorithm will take the labeled_rgb_path which is the path that contains all the rgb images that have been 
    labeled with measurements. This path is in the results folder.
    Output path, the location where the final reconstructed image will be stored
    """

    print(labeled_rgb_path)
    print(reconstructed_output_path)